# Mignone Labs website refresh — September 9, 2026

## Published through WordPress

The existing URLs used by released apps are preserved.

| WordPress page | ID | Path |
|---|---:|---|
| Home | 9 | / |
| CarryAwareNJ Support | 16 | /carryawarenj-support/ |
| CarryAwareNJ Privacy Policy | 22 | /carryawarenj-privacy/ |
| CarryAwareNJ Roadmap | 28 | /carryaware-roadmap/ |
| CarryAwareNJ Release Notes | 32 | /carryaware-release-notes/ |
| CarryAwareNJ product landing page | 80 | /carryawarenj/ |
| FlockAware product preview | 81 | /flockaware/ |

Pages use the existing page-no-title template and one shared HTML/CSS design system in the database-stored header template part (43). The footer template part (41) is compact on utility pages. WordPress retains the publishing records; this document is not a full website backup or a mirror of every page.

Official CarryAwareNJ App Store icon and screenshots were imported as media IDs 75–78 with alt text and explicit dimensions. No simulated interface image, invented rating, pricing, trial, TestFlight link, or FlockAware public-release claim was added. FlockAware is labeled a product preview, separate from CarryAwareNJ.

## Release data source and cache

The live WordPress `Mignone Labs Core` Code Snippets entry reads Apple's public lookup API directly and keeps a six-hour transient. It is request-driven, not an independent six-hour cron task. LiteSpeed's seven-day full-page cache had hidden a newer Apple result; `/carryaware-release-notes/` is now excluded from that HTML cache. The normal public page returned 1.0.5 after the correction.

GitHub separately preserves older releases. The archive includes 1.0.5 (Apple UTC timestamp 2026-09-09T01:12:17Z, equivalent to September 8 in US Eastern time) and 1.0.4 from the previously captured public release page. Unknown older releases were not invented.

`scripts/sync_app_releases.py` supports configured app JSON files, validates the Apple ID, numeric version, timezone-bearing release date, notes, and App Store host; preserves prior releases; rejects a feed that would move backwards; and uses bounded request retries and atomic local writes. The workflow runs at minute 17 every six hours, on relevant code changes, or manually. A scheduled start is not an exact delivery-time guarantee.

Verification: GitHub Actions run 34389362575 completed successfully. All 12 tests passed. The real Apple lookup returned 1.0.5; the no-change path correctly avoided a redundant commit. A future changed-version push is configured but was not artificially forced in this run.

## Prepared, NOT live

A draft theme `twentytwentyfour-wpvibe-draft` contains `inc/mignone-labs-website.php` and a functions.php include. It is intended to add escaped semantic release-note bullets, a validated GitHub history display with last-good fallback, and server-rendered description/Open Graph/Twitter metadata from page excerpts. The release page contains an empty history slot for this integration.

The PHP file passed the site's syntax check. HOWEVER the preview did not execute the new hooks and did not preserve the custom database-stored template parts. Do not publish this draft until the preview behavior is resolved and the actual rendered results are verified. A parent-theme update could replace theme code; move durable functionality into a maintained child theme/plugin in the approved deployment path rather than relying on untracked parent-theme modifications.

Theme-file publication requires a working preview, explicit post-preview publish approval, and confirmation of a recent full site backup. The theme-only safety copy is not a database backup. No completed full-site backup has been verified in this task.

## Required follow-up checks

- Reconcile the published privacy wording with the released build, deployed Firebase configuration, service retention settings, and App Store privacy labels. Source code review is not production verification or legal certification.
- Finish archive rendering, semantic bullet formatting, and server-side metadata deployment; then inspect public output and sharing previews.
- Test actual iPhone Safari, keyboard navigation, native menu/FAQ controls, text scaling, and horizontal overflow. Responsive CSS and focus styles are implemented, but no physical-device or Lighthouse audit has been completed.
- Confirm FlockAware release status and final pricing before replacing preview availability language. Do not reuse CarryAwareNJ's policy as FlockAware documentation.

## TOP backlog immediately after website

CarryAwareNJ issue #10: https://github.com/mattymigs/CarryAwareNJ/issues/10

Matt explicitly requested the hard-coded/static in-app content audit as the top follow-up. Review installed version/build labels, help/onboarding/privacy/account text, resource/support URLs, static availability and data counts, and pricing copy. Use safe local/bundled fallbacks for approved remote content. No app code was changed during this website task. This issue is a durable backlog record, not a scheduled notification.
