# Mignone Labs Site 1.0.0 — public deployment, September 9, 2026

## Owner approval and change

After reviewing the installed plugin preview, Matt explicitly approved: “publish it, and give me link to send to my friend/colleague.”

The connected WordPress preview endpoint initially reported `preview-only`, version `1.0.0`, theme `twentytwentyfour`, with the existing Apple-data provider available. No matching plugin write ability was registered. The single approved configuration change was made through WordPress's option API: `option update mls_live true --format=json`. The tool reported success and automatic LiteSpeed/object-cache purging. No extra manual purge was issued.

The same authenticated endpoint then reported `mode: live`. No theme was replaced or published; the old WPVibe theme draft remains unused. This supersedes the earlier plugin-not-live status in rollout notes.

## Verification

WPVibe `get_page_html` fetched ordinary URLs with no preview parameters:

- `https://mignonelabs.com/carryaware-release-notes/`: version 1.0.5, release notes rendered as a semantic nine-item list, and Previous releases containing an expandable 1.0.4 entry dated 2026-08-31.
- `https://mignonelabs.com/`: the refreshed product sections, official CarryAwareNJ icon/screenshots, direct App Store download links, and separate FlockAware preview link remained present.
- Homepage head: description, Open Graph title/description/URL/image, and Twitter summary metadata were present. The sampled robots tag did not contain noindex.

The external web-retrieval tool returned an older crawl and could not fetch fresh query variants. A separate container HTTP check was unavailable due to its DNS failure. These are not counted as successful independent public-site tests. The successful rendered-page checks above were through the connected WPVibe normal-URL fetch, not its authenticated component preview endpoint.

No new physical-device, accessibility, performance, or third-party social-preview test was performed in this publishing step. Metadata presence does not guarantee that another service immediately refreshes a previously cached preview.

## Recovery and next work

The owner previously confirmed a locally downloaded full cPanel archive; their `gzip -t` and `tar -tzf` checks passed. That is an archive-readability check, not a tested restoration. The private backup is not committed here.

Rollback: clear the plugin's public-mode checkbox in Tools → Mignone Labs Site, or deactivate the plugin. Original pages and release shortcode remain intact.

This repository records the helper plugin and release-data automation; it is still not a full WordPress database/media/page backup. Remaining consistency checks from the rollout notes are not implicitly completed by enabling this plugin.

Top next app backlog: CarryAwareNJ issue #10, audit and update static/hard-coded in-app content. No app code was changed during this publish.
